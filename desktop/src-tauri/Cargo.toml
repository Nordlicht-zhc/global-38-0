[package]
name = "global-38-0"
version = "1.0.0"
description = "Global 38-0 football draft and simulation game"
authors = ["Global 38-0"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

